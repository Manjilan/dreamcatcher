<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Dream Catcher</title>
  <link href="https://fonts.googleapis.com/css?family=Lato|Raleway" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="../../styles/style.css">
  <!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0-beta/css/materialize.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0-beta/js/materialize.min.js"></script> -->
  <script src="../../scripts/app.js"></script>
</head>
<body>
  <nav class="nav">
    <a href="/"><img src="../../assets/dreamcatcherlogo.png" class="logo"></a>
    <a href="/">Home</a>
    <a href="/#about">About</a>
    <a href="/api/journalposts">Gallery</a>
    <% if (!user) { %>
    <a href="/signup">Sign Up</a>
    <a href="/login">Log In</a>
    <% } else { %>
    <a href="/users/<%=user.id%>"><%= user.username %></a>
    <a href="/logout">Log Out</a>
    <% } %>
  </nav>
  <!-- <header>
  </header> -->
  <main>
    <h2><%= post.date %></h2>
    <h2><%= post.title %></h2>
    <p> <%= post.description %></p>
    <% for (let comment of comments){ %>
      <div class="commentBox">
        <h4><%= comment.user %> said: </h4>
        <h5><%= comment.comment %></h5>
      </div>
    <% } %>
    <% if (user) { %>
    <form method="POST" action='/api/comments'>
      <label> <%= user.username %> <input type="text" placeholder="comment" name="comment" data-id="<%= post.id%>" id="comments"></label>
      <input type="hidden" name="post_id" value=<%= post._id %> />
      <input type="submit">
    </form>
    <% } %>
  </main>
  <footer>
    © Dream Catcher
  </footer>
</body>
</html>
